{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@go-like/server",
  "version": "0.0.1",
  "description": "Go-style internal service server for go-like transports.",
  "type": "module",
  "module": "src/index.ts",
  "typings": "src/index.ts",
  "sideEffects": false,
  "publishConfig": {
    "directory": "dist",
    "access": "public"
  },
  "files": [
    "dist"
  ],
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "bun x --bun tsdown --config-loader native",
    "typecheck": "tsc -p tsconfig.test.json --pretty false",
    "test:unit": "bun test --isolate --no-orphans test/*.test.ts",
    "test:unit:coverage": "bun test --isolate --no-orphans test/*.test.ts --coverage --coverage-reporter=lcov --coverage-dir .artifacts/coverage"
  },
  "dependencies": {
    "@go-like/context": "0.0.1",
    "@go-like/core": "0.0.1",
    "@go-like/metadata": "0.0.1",
    "@go-like/resilience": "0.0.1",
    "@go-like/struct": "0.0.1",
    "@go-like/transport": "0.0.1"
  },
  "devDependencies": {
    "@go-like/transport-memory": "workspace:*"
  },
  "license": "MIT"
}
