{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@likego/metadata",
  "version": "0.0.1",
  "description": "Portable multi-value Context metadata for LikeGo.",
  "type": "module",
  "module": "src/index.ts",
  "typings": "src/index.ts",
  "sideEffects": false,
  "publishConfig": {
    "directory": "dist",
    "access": "public"
  },
  "files": [
    "dist"
  ],
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "bun x --bun tsdown --config-loader native",
    "test": "bun test --isolate --no-orphans test/*.test.ts",
    "test:coverage": "bun test --isolate --no-orphans test/*.test.ts --coverage --coverage-reporter=lcov --coverage-dir .artifacts/coverage && bun test/coverage-contract.ts",
    "typecheck": "tsc -p tsconfig.test.json --pretty false",
    "smoke:bun": "bun test/smoke/package-smoke.ts",
    "smoke:node": "tsx test/smoke/package-smoke.ts",
    "smoke:deno": "deno run --sloppy-imports --config ../../deno.json test/smoke/package-smoke.ts"
  },
  "dependencies": {
    "@likego/context": "0.0.1"
  },
  "license": "MIT"
}
