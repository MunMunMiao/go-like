{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@likego/prometheus",
  "version": "0.0.1",
  "description": "Application-owned prom-client registries and explicit LikeGo request instrumentation.",
  "type": "module",
  "module": "src/index.ts",
  "typings": "src/index.ts",
  "sideEffects": false,
  "publishConfig": {
    "directory": "dist",
    "access": "public"
  },
  "files": [
    "dist"
  ],
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "bun x --bun tsdown --config-loader native",
    "typecheck": "tsc -p tsconfig.test.json --pretty false",
    "test:unit": "bun test --isolate --no-orphans test/*.test.ts",
    "test:unit:coverage": "bun test --isolate --no-orphans test/*.test.ts --coverage --coverage-reporter=lcov --coverage-dir .artifacts/coverage",
    "test:e2e:runtimes": "bun test/e2e/runtime.ts && tsx test/e2e/runtime.ts && node ../../e2e/scripts/prometheus-native.ts"
  },
  "dependencies": {
    "@likego/broker": "0.0.1",
    "@likego/client": "0.0.1",
    "@likego/context": "0.0.1",
    "@likego/server": "0.0.1",
    "@likego/transport": "0.0.1",
    "@likego/web": "0.0.1",
    "prom-client": "15.1.3"
  },
  "devDependencies": {
    "@types/node": "26.1.1"
  },
  "license": "MIT"
}
